CHIP DecoderSegment201498887 {
  IN A, B, C, D;
  OUT b;

  PARTS:
  NOT (in=A, out=nA);
  NOT (in=B, out=nB);
  NOT (in=C, out=nC);
  NOT (in=D, out=nD);

  AND(a=A, b=B, out=b1);
  AND(a=A, b=C, out=b2);
  AND(a=b2, b=D, out=b3);
  AND(a=C, b=nD, out=b4);
  AND(a=nC, b=nD, out=b5);
  AND(a=b5, b=nB, out=b6);

  OR(a=b1, b=b4, out=b7);
  OR(a=b3, b=b6, out=b8);
  OR(a=b7, b=b8, out=b);
}
