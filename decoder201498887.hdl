Chip decoder201498887 {
  In A, B, C, D;
  Out b;

  Parts:
  Not (in=A, out=nA);
  Not (in=B, out=nB);
  Not (in=C, out=nC);
  Not (in=D, out=nD);

  And(a=A, b=B, out=b1);
  And(a=A, b=C, out=b2);
  And(a=b2, b=D, out=b3);
  And(a=C, b=nD, out=b4);
  And(a=nC, b=nD, out=b5);
  And(a=b5, b=nB, out=b6);

  Or(a=b1, b=b4, out=b7);
  Or(a=b3, b=b6, out=b8);
  Or(a=b7, b=b8, out=b);
}
